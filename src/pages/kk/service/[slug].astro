---
// Автогенерация (scripts/make-kk-pages.mjs): казахская версия страницы service/[slug].astro. Не редактировать вручную.
import Page from '../../service/[slug].astro';
import { getStaticPaths as ruPaths } from '../../service/[slug].astro';
export const getStaticPaths = ruPaths;
---
<Page {...Astro.props} />
