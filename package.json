{
  "name": "interna-clinic",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/make-kk-pages.mjs && astro dev",
    "build": "node scripts/make-kk-pages.mjs && astro build && node scripts/apply-kk.mjs && node scripts/apply-base.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.0.0"
  }
}
